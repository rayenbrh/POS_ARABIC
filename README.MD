🛒 نظام نقطة البيع وإدارة المخزون - POS & Inventory System
نظام متكامل لإدارة نقاط البيع والمخزون باللغة العربية (RTL) مع واجهة حديثة وسهلة الاستخدام.

✨ المميزات الرئيسية
🔐 المصادقة والصلاحيات
نظام تسجيل دخول آمن باستخدام JWT
صلاحيات متعددة (Admin / Cashier)
حماية المسارات حسب الصلاحيات
📦 إدارة المنتجات
إضافة وتعديل وحذف المنتجات
دعم أنواع بيع متعددة (بالقطعة، بالكيلوجرام، بالكوب)
تحديد وزن الكوب القابل للتعديل
تنبيهات المخزون المنخفض
حساب قيمة المخزون تلقائياً
💰 نقطة البيع (POS)
واجهة سريعة وسهلة للكاشير
تصفية المنتجات حسب الفئات
إضافة المنتجات للسلة بطرق مختلفة
حساب المبلغ المدفوع والباقي تلقائياً
طباعة الفواتير
سجل المبيعات اليومي
إمكانية حذف الفواتير (Soft Delete)
📊 إدارة المخزون
إضافة وخصم وتعديل المخزون
تتبع حركات المخزون
تحديث المخزون تلقائياً عند البيع
تنبيهات عند انخفاض المخزون
📈 التقارير المالية
تقرير مالي شامل (المبيعات، التكاليف، الأرباح)
المبيعات حسب المنتج
المصروفات
هامش الربح
عدد المعاملات ومتوسط الفاتورة
💸 إدارة المصروفات
تسجيل المصروفات من الصندوق
تصنيف المصروفات
تقارير المصروفات
🛠️ التقنيات المستخدمة
Backend
Node.js - بيئة التشغيل
Express.js - إطار العمل
MongoDB Atlas - قاعدة البيانات
Mongoose - ODM للتعامل مع MongoDB
JWT - المصادقة والتوكنات
bcrypt - تشفير كلمات المرور
Frontend
React 18 - مكتبة واجهة المستخدم
Vite - أداة البناء السريعة
TailwindCSS - إطار CSS
React Router - التنقل بين الصفحات
Axios - طلبات HTTP
React Hot Toast - الإشعارات
📋 المتطلبات
Node.js (v16 أو أحدث)
MongoDB Atlas حساب مجاني
npm أو yarn
🚀 التثبيت والتشغيل

## وضع التطوير (Development)

1️⃣ إعداد قاعدة البيانات
- إنشاء حساب مجاني على MongoDB Atlas
- إنشاء Cluster جديد
- إنشاء Database User
- الحصول على Connection String

2️⃣ إعداد Backend
```bash
# الانتقال إلى مجلد Backend
cd backend

# تثبيت المكتبات
npm install

# تعديل ملف .env
PORT=5000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pos_db
JWT_SECRET=your_super_secret_key_here
JWT_EXPIRE=7d
NODE_ENV=development

# تشغيل الخادم
npm run dev
```

3️⃣ إعداد Frontend
```bash
# الانتقال إلى مجلد Frontend
cd frontend

# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm run dev
```
التطبيق سيعمل على: http://localhost:3000

## وضع الإنتاج (Production) - Single Service Deployment

هذا التطبيق مُعد للعمل كخدمة واحدة حيث يخدم Backend كل من API و Frontend.

### Build و Deploy

```bash
# 1. Build Frontend
cd frontend
npm install
npm run build
# سيتم إنشاء الملفات في backend/public/

# 2. Démarrage du Serveur
cd ../backend
npm install
npm start
```

### Déploiement sur Hostinger EasyPanel

1. **Créer un seul service** dans EasyPanel
2. **Point d'entrée**: `backend/server.js`
3. **Commande de démarrage**: `npm start`
4. **Port**: Configuré automatiquement par EasyPanel
5. **Variables d'environnement**:
   - `PORT` (optionnel, par défaut 5000)
   - `MONGODB_URI` (requis)
   - `JWT_SECRET` (requis)
   - `JWT_EXPIRE` (optionnel, par défaut 7d)

### Structure des Routes

- **API**: `/api/*` → Routes Express
- **Frontend**: `/*` → React App (index.html)
- **Assets**: `/assets/*` → Fichiers statiques

Voir `DEPLOYMENT.md` pour plus de détails.

👥 إنشاء المستخدمين الأوليين
يجب إنشاء مستخدم Admin أولاً عبر MongoDB Compass أو MongoDB Shell:

javascript
// اتصل بقاعدة البيانات وأدخل:
db.users.insertOne({
  name: "المدير",
  email: "admin@pos.com",
  passwordHash: "$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5/z5rE5rE5rE5", // admin123
  role: "admin",
  createdAt: new Date(),
  updatedAt: new Date()
})
ملاحظة: يجب تشفير كلمة المرور باستخدام bcrypt. يمكنك استخدام:

javascript
const bcrypt = require('bcrypt');
const hash = await bcrypt.hash('admin123', 12);
console.log(hash);
حسابات التجربة
مدير:

البريد: admin@pos.com
كلمة المرور: admin123
كاشير:

البريد: cashier@pos.com
كلمة المرور: cashier123
📱 الصفحات والمميزات
لوحة التحكم
عرض إحصائيات اليوم
تنبيهات المخزون المنخفض
ملخص المبيعات والأرباح
نقطة البيع (POS)
تبويب البيع: واجهة سريعة للبيع
تبويب السجل: عرض وحذف المبيعات اليومية
إدارة المنتجات
إضافة منتجات جديدة
تعديل بيانات المنتجات
حذف المنتجات
دعم أنواع بيع متعددة
إدارة الفئات
إنشاء فئات المنتجات
تعديل وحذف الفئات
إدارة المخزون
عرض جميع المنتجات ومخزونها
إضافة مخزون جديد
خصم مخزون
تعديل المخزون
تتبع قيمة المخزون
التقارير
تقارير مالية شاملة
المبيعات حسب المنتج
تقارير المصروفات
اختيار الفترة الزمنية
إدارة المستخدمين
إضافة مستخدمين جدد
تحديد الصلاحيات
الفواتير المحذوفة
عرض جميع الفواتير المحذوفة
معلومات تفصيلية عن كل فاتورة
🔒 الأمان
تشفير كلمات المرور باستخدام bcrypt
JWT tokens للمصادقة
حماية المسارات حسب الصلاحيات
Middleware للتحقق من الصلاحيات
CORS محمية
💾 قاعدة البيانات
المجموعات (Collections)
users - المستخدمين
categories - الفئات
products - المنتجات
sales - المبيعات
stockmovements - حركات المخزون
expenses - المصروفات
📊 MongoDB Transactions
النظام يستخدم MongoDB Transactions لضمان سلامة البيانات في:

إنشاء عمليات البيع وتحديث المخزون
حذف الفواتير وإرجاع المخزون
تحديثات المخزون
🌐 API Endpoints
المصادقة
POST /api/auth/login - تسجيل الدخول
POST /api/auth/users - إنشاء مستخدم (Admin)
GET /api/auth/users - جلب المستخدمين (Admin)
الفئات
GET /api/categories - جلب الفئات
POST /api/categories - إنشاء فئة (Admin)
PUT /api/categories/:id - تحديث فئة (Admin)
DELETE /api/categories/:id - حذف فئة (Admin)
المنتجات
GET /api/products - جلب المنتجات
GET /api/products/:id - جلب منتج
POST /api/products - إنشاء منتج (Admin)
PUT /api/products/:id - تحديث منتج (Admin)
DELETE /api/products/:id - حذف منتج (Admin)
المبيعات
POST /api/sales - إنشاء عملية بيع
GET /api/sales - جلب المبيعات
GET /api/sales/:id - جلب عملية بيع
DELETE /api/sales/:id - حذف عملية بيع
GET /api/sales/deleted - جلب المبيعات المحذوفة (Admin)
المخزون
POST /api/stock/move - تحديث المخزون (Admin)
GET /api/stock/movements - جلب حركات المخزون
المصروفات
POST /api/expenses - إضافة مصروف
GET /api/expenses - جلب المصروفات
DELETE /api/expenses/:id - حذف مصروف
التقارير
GET /api/reports/financials - التقرير المالي (Admin)
GET /api/reports/low-stock - منتجات المخزون المنخفض
GET /api/reports/sales-by-product - المبيعات حسب المنتج (Admin)
🎨 التصميم
واجهة عربية كاملة (RTL)
تصميم حديث وجذاب
ألوان متناسقة
استجابة كاملة لجميع الشاشات
رموز تعبيرية (Emojis) لسهولة التعرف
رسائل توست للإشعارات
🐛 معالجة الأخطاء
رسائل خطأ واضحة بالعربية
معالجة الأخطاء في Frontend و Backend
Validation للبيانات المدخلة
Toasts للإشعارات
📝 ملاحظات مهمة
تأكد من تشغيل MongoDB Atlas
تأكد من صحة Connection String
قم بإنشاء مستخدم Admin أولاً
استخدم TND كعملة
جميع النصوص بالعربية
النظام يدعم RTL بالكامل
🔄 التحديثات المستقبلية
 تصدير التقارير إلى PDF/Excel
 نظام الباركود
 تطبيق موبايل
 تقارير متقدمة
 نظام الولاء للعملاء
 دعم عدة فروع
 الدفع الإلكتروني
📞 الدعم
للمساعدة أو الاستفسارات، يرجى فتح Issue في المشروع.

📄 الترخيص
هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

تم التطوير بـ ❤️ في تونس 🇹🇳

